enum LocationPermissionLevel {
  location,
  locationWhenInUse,
  locationAlways,
}

enum PermissionStatus {
  unknown,
  denied,
  granted,
  restricted,
  whenInUse,
}

enum ServiceStatus {
  unknown,
  disabled,
  enabled,
  notApplicable,
  whenInUse,
}