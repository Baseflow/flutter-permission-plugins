enum LocationPermissionLevel {
  location,
  locationWhenInUse,
  locationAlways,
}

enum PermissionStatus {
  unknown,
  denied,
  granted,
  restricted,
}

enum ServiceStatus {
  unknown,
  disabled,
  enabled,
  notApplicable,
}
