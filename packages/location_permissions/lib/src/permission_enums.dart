enum LocationPermissionLevel {
  location,
  locationWhileInUse,
  locationAlways,
}

enum PermissionStatus {
  unknown,
  denied,
  granted,
  restricted,
  whileInUse,
}

enum ServiceStatus {
  unknown,
  disabled,
  enabled,
  notApplicable,
  whileInUse,
}